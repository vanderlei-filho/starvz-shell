This repository contains a Containerfile with everything needed to handle prof files generated by StarPU and FxT.

Use `make help` for instructions.

```shell
# Convert FxT to Paje format:
starpu_fxt_tool -i *

# Then run both phases:
starvz -1 -t .
starvz -2 .

# To run custom scripts:
Rscript /workspace/gantt.R
```
